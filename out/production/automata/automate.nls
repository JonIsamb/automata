;; NetLogo code automatically generated
;; Automaton: 10 states

to stop
  ;; to be completed
  print "stop"
end

to move
  ;; to be completed
  print "move"
  if (button-quit = true) [
    set next-activity [ -> quit ]
  ] [
    if (button-stop = true) [
      set next-activity [ -> stop ]
    ] [
      if (button-north = true) [
        set next-activity [ -> heads_north ]
      ] [
        if (button-west = true) [
          set next-activity [ -> heads_west ]
        ] [
          if (button-south = true) [
            set next-activity [ -> heads_south ]
          ] [
            if (button-east = true) [
              set next-activity [ -> heads_east ]
            ] ] ] ] ] ]
end

to heads_north
  ;; to be completed
  print "heads_north"
end

to heads_west
  ;; to be completed
  print "heads_west"
end

to heads_south
  ;; to be completed
  print "heads_south"
end

to heads_east
  ;; to be completed
  print "heads_east"
end

to jump
  ;; to be completed
  print "jump"
end

to pick_up
  ;; to be completed
  print "pick_up"
end

to drop
  ;; to be completed
  print "drop"
end

to quit
  ;; to be completed
  print "quit"
end

